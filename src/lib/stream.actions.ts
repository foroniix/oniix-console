export async function stopStream(id: string) {
  // POST /streams/:id/stop
}

export async function restartStream(id: string) {
  // POST /streams/:id/restart
}

export async function muteStream(id: string) {
  // POST /streams/:id/mute
}
